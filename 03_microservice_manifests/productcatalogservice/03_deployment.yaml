apiVersion: apps/v1
kind: Deployment
metadata:
  name: productcatalogservice
  labels:
    app.kubernetes.io/name: productcatalogservice
    app.kubernetes.io/instance: productcatalogservice
    app.kubernetes.io/component: service
    app.kubernetes.io/owner: online-boutique
spec:
  replicas: 3
  strategy:
    rollingUpdate:
      maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app.kubernetes.io/name: productcatalogservice
      app.kubernetes.io/instance: productcatalogservice
      app.kubernetes.io/component: service
      app.kubernetes.io/owner: online-boutique
  template:
    metadata:
      labels:
        app.kubernetes.io/name: productcatalogservice
        app.kubernetes.io/instance: productcatalogservice
        app.kubernetes.io/component: service
        app.kubernetes.io/owner: online-boutique
    spec:
    # TOPOLOGY SPREAD CONSTRAINTS - Distribute pods across nodes AND availability zones
      topologySpreadConstraints:
        # Spread across nodes (within same AZ)
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: productcatalogservice
              app.kubernetes.io/component: service
              app.kubernetes.io/instance: productcatalogservice
        
        # Spread across availability zones
        - maxSkew: 1
          topologyKey: topology.kubernetes.io/zone
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: productcatalogservice
              app.kubernetes.io/component: service
              app.kubernetes.io/instance: productcatalogservice    
      # THIS IS CRITICAL - Forces pods to run ONLY on spot nodes
      nodeSelector:
        karpenter.sh/capacity-type: spot
      serviceAccountName: productcatalogservice
      securityContext:
        fsGroup: 1000
      containers:
        - name: productcatalogservice
          envFrom:
            - configMapRef:
                name: productcatalogservice
          securityContext:
            capabilities:
              add:
              - NET_BIND_SERVICE
              drop:
              - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          image: 927749346049.dkr.ecr.ap-southeast-1.amazonaws.com/online-boutique/productcatalogservice:v0.0
          imagePullPolicy: IfNotPresent
          ports:
          - containerPort: 3550
          readinessProbe:
            periodSeconds: 5
            grpc:
              port: 3550
          livenessProbe:
            periodSeconds: 5
            grpc:
              port: 3550
          resources:
            limits:
              memory: 512Mi
            requests:
              cpu: 128m
              memory: 512Mi
          volumeMounts:
            - mountPath: /tmp
              name: tmp-volume
      volumes:
        - name: tmp-volume
          emptyDir:
            medium: Memory

